extends includes/layout.pug
block content
  section(class='cl-clients')
    .container 
      .co-main
        each val in clientsImgs
          .co-img
            img.h-100(src=`${img}clients/${val}`, alt="")
        